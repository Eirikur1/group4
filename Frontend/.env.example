# Copy this file to .env or .env.local and fill in your values.
# The app loads .env (Expo also loads .env.local). Do not commit .env or .env.local.

# Supabase (for Storage photo uploads from the app)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Backend API (water_sources read/write).
# Production: use your Vercel backend URL, e.g. https://your-backend.vercel.app
# Local simulator: http://localhost:3000
# Local device: your computer's IP, e.g. http://192.168.1.5:3000 (same WiFi; backend must be running).
EXPO_PUBLIC_API_URL=http://localhost:3000