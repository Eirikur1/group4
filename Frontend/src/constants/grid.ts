/**
 * Layout grid: 4 columns, margin 16, gutter 16.
 * Use GRID_MARGIN for screen-edge padding; use GRID_GUTTER for spacing between elements.
 */
export const GRID_COLUMNS = 4;
export const GRID_MARGIN = 16;
export const GRID_GUTTER = 16;

/** Half gutter for tighter spacing (e.g. between icon and text). */
export const GRID_GUTTER_HALF = GRID_GUTTER / 2;
